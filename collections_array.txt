
Initialization
    => var a[] = new int[];

Methods 
    => Copy of array
    => b = a.clone()
    => c = Arrays.copyOf(a, end_pos+1);


Functions
    => Set default values 
    => Arrays.fill(numbers, 42);   // Fill all elements with the value 42
    => Sorting
    => Array.sort(k);
    => Array.sort(k, Collections.reverseOrder()); // Not works for char array