List 
path.remove(path.size()-1);

Stack
q.pop()

Queue
q.poll()

LinkedHashSet
Iterator<String> it = set.iterator();
if (it.hasNext()) {
    it.next();    // move to first element
    it.remove();  // remove it
}
